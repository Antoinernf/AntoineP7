// AppContext : C'est un component qui permet de créer un useContext avec React pour la gestion des JWT utilisateur
import { createContext } from 'react';

export const UidContext = createContext(); // Stocké vide dans notre projet par défaut